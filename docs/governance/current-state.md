---
Estado: "referencia"
Rol: "Describir el pulso real de la estructura física y funcional del repositorio"
Version: "v0.2.0"
Impacto_ID: "SSoT-State"
Fidelidad_Fisica: "Project/src/"
Fecha_de_creacion: "2026-04-18"
Fecha_de_actualizacion: "2026-07-02 (actualización #17)"
---

# OmniFrame — Estado Actual

> **Audit fecha:** 2026-05-18 | **Actualización parcial:** 2026-05-19 (correcciones EnsembleAdapter, modelo de capas) | **Actualización #4:** 2026-06-01 (gramática de nomenclaturas)
> **Metodología:** Lectura directa del código físico. Este documento reemplaza la versión anterior (v0.0.3) que tenía drift significativo con el estado real.

> **[2026-06-01] Gobernanza:** Nueva SSoT de nomenclaturas establecida. Ver [`docs/governance/nomenclature-grammar.md`](nomenclature-grammar.md). Todos los tags inline en JSONs y docs han sido migrados a la gramática `DOMINIO:ROL[:ESQUEMA/ID]`. La colisión `[engine]`/`[ENGINE]` queda resuelta.

> **[2026-06-01] Gobernanza (D-19):** Redefinida la naturaleza de `condition`. Es vocabulario **endógeno** (no proviene de `@wfcd/items`, a diferencia de `upgrade_*`): el SSoT del token es el override JSON; `docs/semantic/conditions.md` es **consolidador posterior**, no portero previo. Un token capturado y aún no consolidado en el doc = cola de consolidación, no drift. `notes[]` queda definido como capa de anotación/auditoría, nunca SSoT. Ver [`docs/data/decisions.md`](../data/decisions.md) D-19.

> **[2026-06-08] Engine — metodología de validación (prototipo VIGENTE):** Graduada a [`attribute-node-contract.md`](../domains/engine/attribute-node-contract.md) la metodología de test progresivo + derivación: los buckets del `AttributeNode` (`base_add_pct`, `mods_add_pct`, `multiplicative`, …) son la **superficie de aserción** del test (test de lógica vs. test de estabilidad), el fixture es una `EnsembleIntention` + cadena de derivación esperada por nodo, y la base del linaje debe ser incondicional. Es prototipo con base documentada. La build de referencia (Rhino), su estratificación y el lineaje de decisión (D12–D16) están en [`engine/test/`](../domains/engine/test/) (ver entrada 2026-06-09); la validación con ≥2 warframes adicionales sigue abierta.

> **[2026-06-09] Engine — testing derivado ejercido + sub-área `engine/test/`:** La metodología pasó de prototipo a ejercida sobre **4 consumidores de arma** (Boltor, Cedo, Felarx, Laetum) vía el "clic" (`__tests__/helpers/consume.ts`): un `consume()` por intención, estabilidad (`.final`) + lógica (buckets) en la misma fuente. Se acuñó la **gramática ✓/fails/todo** (el `it.todo` mapea el borde C1↔C2). `validation-builds.md` se eliminó y su contenido se repartió en **`docs/domains/engine/test/`**: `test-workflow.md` (CÓMO + registro de decisión), `catalog-current.md` (índice de consumidores), `catalog-future.md` (Rhino, standard-set, frontera C2). **Decisión durable** en `test-workflow.md`: la observabilidad por buckets es intención de diseño original (orientada a D, diferida); la sonda de construcción es la función emergente; el agnosticismo de capas es el invariante a proteger. Disparadores de graduación: abilities → dominio docs (Futuro 2); D existe → capa observabilidad ≈ `observer/` (Futuro 3). C2 no gradúa (crece dentro de engine).

> **[2026-06-10] Engine — frontera C→D + oráculo CLI (decisiones + prototipo):** Sesión de diseño materializada. Decisiones (ver [`engine/design/arch-decisions.md`](../domains/engine/design/arch-decisions.md) §5-7): (1) oráculo del motor = **CLI, no MCP** (MCP diferido); (2) `consume()` = **salida de C**, promovido a módulo en `@core/engine/output/` (fuera de `__tests__/`, 2026-06-10) — superficie del dominio engine, consumida por scripts/tests (no-dominios); **no es Capa D**; (3) **frontera de dominios**: los dominios no importan `@core` (reafirma Restricción 1) — la UI y la Capa D (consumo derivado, `ViewModelContract`) cruzan por `@shared`. `C→D→UI` = **prototipo en revisión**. Diseño activo de `ViewModelContract` (consumer-shaped) + simetría de entrada en `OQ-ENGINE-FUTURE` ([`open-questions.md`](open-questions.md)).
> **Deuda registrada (sesión 2026-06-10):**
> - `domains/arsenal/view/UpgradeView.tsx` importa `@core/engine/hooks/useSimulation` directo → **violación de la frontera de dominios** (stub conectado antes de existir D); corregir vía `@shared` al materializar D. **[RESUELTO 2026-06-12]** consume el `ViewModelContract` vía `useViewModel` (`@providers`); ya no importa `@core`.
> - `useSimulation` en `@core/engine/hooks` = D reactiva parcial co-ubicada en `@core` → drift a reubicar fuera de `@core` cuando D se materialice.

> **[2026-06-11] Engine — oráculo CLI v0 + derivación de tests (contrato + módulos):** `consume()` extendido con `snapshot(): SimulationEntity[]` (salida cruda de C, forma-de-productor; **cambio de contrato del puerto**), promovido a `@core/engine/output/consume.ts`. **Harness de entrada** en `@core/engine/fixtures/`: `loadEngineData()` (bootstrap, ex-`__tests__/helpers/engine-data-setup`) + `builds.ts` (catálogo de las 5 builds verificadas + registro `BUILDS`). **Oráculo CLI** (`scripts/oracle/`, `npm run oracle -- <build>` | `all`): adaptador no-reactivo que inspecciona el snapshot crudo — primer cliente real consumiendo el motor (no-UI). Test (asertar) y oráculo (inspeccionar) = adaptadores hermanos sobre el mismo input; el oráculo NO reinventa el runner. `ViewModelContract`/Capa D siguen **diferidos** (sin UI). Nuevas OQ: **OQ-ENGINE-8** (sobrecarga "Proyección"/`ProjectionSnapshot`), **OQ-ENGINE-9** (estructura interna de `@core` + `fixtures/` mixto). Ver [`engine/design/arch-decisions.md`](../domains/engine/design/arch-decisions.md) §5-7 y [`open-questions.md`](open-questions.md).
> - La doc de capas atribuye el "DNA Mutation Step" (shards/helminth) a la Capa B, pero el código lo ejecuta en C1 (`StaticHydrator`); B solo mapea shards en `ensembleFromIntention` → drift de clasificación menor.
> - Puntero archivado a `docs/design/sim-v2/OMNIFRAME_SIMULATION_ARCHITECTURE.md` (en `docs-archive/legacy/engine/architecture.md` y pre-v1) está **colgado** — la verdad viva es `docs/domains/engine/design/simulation-architecture.md`.

> **[2026-06-12] Engine — reestructura de `@core` (Stage 0+1) ejecutada:** Rama `refactor/core-stage0-restructure`, commit por slice, `tsc -b` CLEAN + 95 tests verde en cada uno. **Stage 0** (reorg interno de `engine/`, blast externo 0): `contracts/index.ts` → `contracts.ts` (cortes/DTOs) + `primitives.ts` (barrel preserva superficie); `combat`+`enemies` → `engine/simulate/` (C2); `resolution`+`hydration` → `engine/resolve/` (C1); `bridge/` → `@core/bridge/` (B, fuera de engine). **Stage 1** (split de A): `ensemble.types` → `@shared/types/ensemble.ts` (gemelo-de-entrada; cierra smell Restricción 1 — 4 dominios importaban `@providers`), `ensemble-store` → `@core/intention/` (A1); `EnsembleProvider` se queda en `@providers`. **Ruling `@providers → @core` PERMITIDO** (capa de composición ≠ dominio de feature). Resuelve mayormente OQ-ENGINE-9 (residuales **gated por D**: extraer `hooks/`, construir A2, split de `fixtures/`). Ver `closed-decisions.md` DC-OQ-ENGINE-9 y `decision-frontier.md` §1.
>
> **[2026-06-12] Engine — `ViewModelContract` v0 (display-only/C1) + Capa D cableada:** Definido `@shared/view-model` (`StatViewModel`/`EntityViewModel`/`ViewModelContract` + `project()`, z2 puro: snapshot crudo → cut C→D; tira los 6 buckets, lleva `value`+`unit`+`category`; label se compone vía i18n en el borde). Test de fidelidad contra snapshot real (Laetum). **D2:** el oráculo gana modo `view` (`npm run oracle -- view <build>`) — primer consumidor. **D1:** `UpgradeView` consume vía `useViewModel` (binding z3 nuevo en `@providers/Ensemble`, liga EnsembleStore→`consume()`→`project()`) y **deja de importar `@core` → ningún dominio importa `@core`** (la frontera 'domains ↛ @core' pasa de aspiracional a cierta). Diferido: i18n/formatter (`lib/*`), `metrics`/A2 (C2), extracción de `hooks/` de `@core`. Rama `refactor/core-stage0-restructure`.
>
> **[2026-06-14] Engine — D-7 Fase 4 (SSoT de presentación + muerte del leak β):** Cerrado el último tramo de D-7 (token D-6 == id de nodo, de C a la UI). `AttributeNode` (`primitives.ts`) **dejó de cargar** `label`/`category`/`unit` — el nodo es **puro** (solo buckets + `final`). `StaticHydrator` ya **no importa** `lib/presentation` (leak β engine→presentación muerto); `getAttributeMetadata` eliminado. `lib/presentation/attribute-registry` reescrito a `Partial<Record<Upgrade, PresentationMeta>>` (key-typed por token D-6, `PresentationMeta = { category, unit }` — la **label es i18n**, no va en el dict; node-id subset, NO el `Record` exhaustivo: los FLAT/BASE/sub-familia convergen y no renderizan). `project()` (`@shared/view-model`) adjunta `unit`+`category` por lookup en el borde C→D (dirección correcta `@shared → @lib`). Gate de `createBaseEntity` simplificado a `isUpgrade()`. Bug visible resuelto (crit/status vuelven a renderizar con `%`). `tsc -b` limpio, suite verde. Cierra faceta SSoT-vocabulario de **OQ-DATA-10** + estrato `lib/format` (vocabulario) de **OQ-ENGINE-10**; sigue abierto: convenciones numéricas, ruta catálogo (`stat-labels`), construir E + renombrar D. Ver [`../data/decisions.md`](../data/decisions.md) §D-7 (COMPLETADA) y [`../domains/ui-ux/presentation-layer.md`](../domains/ui-ux/presentation-layer.md) v0.2.0. Rama `refactor/core-stage0-restructure`.
>
> **[2026-06-14] Presentación — Pre-E cerrado (análisis, sin código) + dirección de la próxima campaña:** Cerrado el frente del proyector de presentación (stages A–D, commits `ea3a601`+`3542242`: proyector único `toStatEntries` en `lib/format`, consumido por D1+D2, gemelos inline muertos). La pasada **Pre-E** (debate RED) concluyó: **la Capa E NO es necesidad emergente** — A–D materializaron el **estrato 2** (`lib/format`), que es lo que se confundía con E, no E; D1 (`UpgradeView`) opera como `D + lib/format` **sin E** y rinde; el chrome (2ª entrada de E) sigue ad-hoc `0→UI`. **E queda estacionado** (modelo válido, gated por un consumidor UI real inexistente — la vista avanzada). Bajado a **OQ-ENGINE-10** (Resultado Pre-E) + **OQ-DATA-10** (Stage E cerrado). **Hallazgo cross-cutting:** la UI nunca tuvo su corpus de docs/auditoría como sí lo tuvo `data/`/engine (asimetría: `docs/semantic/` pesa más que todo `docs/domains/ui-ux/`) → **próximo trabajo = campaña de documentación de UI** (establecer primero el *flujo* de docs de UI — stages/checkpoints — antes de auditar código). Ver `open-questions.md` cluster OQ-UI + OQ-DATA-10/-13.
>
> **[2026-06-14] Gobernanza — Campaña de documentación de UI, Stage 0 (flujo establecido, sin código):** Arrancó la campaña que cierra la asimetría detectada en Pre-E (la UI nunca tuvo corpus de docs/auditoría como data/engine). Stage 0 fue **debate de diseño**, no auditoría de código. Decisiones: **espina = DOMINIO + 2 ejes ortogonales como lente** (estado/intención vs chrome/presentación; rechazadas PROVIDER y VIEW) → nueva serie **U-1**; ui-ux **adopta el trío operativo** que le faltaba (`status.md`+`workflow.md`+`decisions.md`) y "construir" queda fuera del mandato → **U-2**. Creados [`../domains/ui-ux/workflow.md`](../domains/ui-ux/workflow.md) (flujo: loop Recon→Triage→Document, checkpoints C0 ESTABLE-vs-STUB / C1 RED-gate / traceability) y [`../domains/ui-ux/decisions.md`](../domains/ui-ux/decisions.md) (serie U-N, espejo de la D-series, mismo régimen VIGENTE/DEFINITIVA; las decisiones UI previas siguen como anchors `DC-OQ-*`, no re-numeradas). Registro de campaña en [`doc-map.md`](doc-map.md) §6. **`status.md` NO creado** (es output de los barridos por dominio, requieren leer código). Próximo: primer barrido = **arsenal** (espina probada, OQ-UI-2 Stage 0/1/2 ya cerrados). No se abrieron OQ nuevas (reduce, no suma). Ver cluster **OQ-UI** + OQ-DATA-10/-13.
>
> **[2026-06-16] Gobernanza — Campaña UI: cruce de consolidación (paso 3) COMPLETO + reencuadre M1.** La campaña de docs de UI completó sus **6 barridos de código + el cruce de consolidación** (`.working/consolidation-map.md`) y está bajando a docs (paso 4: ver [`../domains/ui-ux/decisions.md`](../domains/ui-ux/decisions.md) **U-3** [3-ejes `SLOT_DEFINITIONS`] / **U-4** [honestidad UI] + **`OQ-ENGINE-11`** [exaltadas]). **Reencuadre arquitectónico clave (M1):** lo que parecían N "futures gateados por un cimiento ausente (la vista avanzada)" **NO es un cimiento ausente** — son **N decisiones de modelado independientes que el flujo A→B→C→D está *diseñado para permitir* (como proyección, NO realidad probada — todo en proceso, nada a prueba de balas), diferidas por prioridad, no bloqueadas** (gates ≠ blockers). Sketches: **multi-config** = modelo *warframe-like* (una entidad → N config slots compartiendo polaridades; badge A/B/C en `/equipment/*`; **NO** builds-separadas estilo overframe.gg; Profile ≠ builds) — gap A, ver §2; **abilities** n%-scaling {fuerza/rango/duración} = sólo cálculo % → **C1 ya las procesa** (el CLI espera el primer warframe+ability); sólo las **ability-LIKE complejas** → fórmulas dedicadas (`formulas/`); **vista avanzada** = la UI clásica 1:1 de Warframe **+ enriquecimientos armónicos** (p. ej. mostrar la fórmula de la habilidad vía los buckets de C), distinta del **builder** (construcción/edición). **Notas de coherencia (auditoría):** (1) el "cierra smell Restricción 1" de [2026-06-12] era de la dirección *dominios→@providers*; la auditoría encontró **otra inversión R1 abierta** — `@shared` importa de `@domains/equipment/hooks/*` (`use-items-filters`/`use-item-details`), a mover a `@shared`. (2) deuda definicional "classic vs advanced vs builder" (se usan sueltos) → asentar en `ui-ux/status.md`. **Fuente:** cruce de consolidación 2026-06-16.
>
> **[2026-06-16] Engine — Saneamiento de `@core`, Fase 0 (purga de muertos pre-oracle/pre-D):** Arranca la campaña de revisión A+B+C (gatillada por el debate sobre la capa B tras consolidar "0" y "D"). **Fase 0 = purga sin diseño** (reduce_chaos antes de rediseñar la frontera de carga). Verificados por ausencia de call-sites y purgados: `@core/engine/hooks/` **completo** (`useSimulation`/`useSimulationMetrics`/`useTimeline` — cluster muerto que solo se referenciaba a sí mismo; **cierra el drift de [2026-06-10]/[2026-06-12]** "reubicar `hooks/` fuera de `@core`" → no se reubica, se purga), `SimulationEngine.getProjectionSnapshot()` + `getAuditSession()`, e interface `ProjectionSnapshot` (con su comentario lápida `// UI Projection Layers`) de `contracts.ts`. **Vivo, intacto:** `AuditStep`/`getAuditResponse()`/`.audit()` (el oráculo los consume — NO son observer muerto). La **intención** de `ProjectionSnapshot` (salida analítica `metrics: { ttk, effective_dps, status_weights }` que la *vista avanzada* consumiría) se **rescató a docs** ([`../domains/ui-ux/status.md`](../domains/ui-ux/status.md) §3, deuda definicional) como boceto/ancla — NO contrato (la forma real se deriva del oráculo/dominio al materializarse). Refinamiento abierto (no purga): el audit se acumula **always-on** en cada `resolve()` aun en el path UI que no lo lee → candidato a **opt-in** (Fase 3). `tsc -b` limpio, 108 tests verde. **Fases siguientes (RED, diseño pendiente):** 1 = "0" como puerto único (2 adapters fetch/import; cierra OQ-DATA-12 + β de OQ-DATA-9), 2 = repos de B → sintetizadores puros sin estado + segmentar Weapon/Warframe + helper `token→Modifier`, 3 = audit opt-in + limpieza de contratos. **Fase 1 en curso (4 slices):** Slice 1 ✅ — seam `DataSource` en `@shared/data` + `NodeAdapter` (fs, tests/CLI) + `StaticAdapter` (import, browser provisional); `loadEngineData(source)` async/agnóstico (no importa adapter concreto → `node:fs` fuera del grafo browser); 11 call-sites Node inyectan `NodeAdapter`. tsc + 108 tests verde. Slice 2 ✅ — `BrowserAdapter` (fetch lazy desde `/data/`) reemplaza a `StaticAdapter` en `main.tsx` (borrado); **bundle 2.3 MB→565 kB (gzip 431→171 kB)**, los 7 JSON salieron del bundle (cierra el síntoma de OQ-DATA-12). Pendiente verificar el render browser real (los tests cubren solo el path Node). Slice 3 ✅ — `DataRegistry` consume el `DataSource` (reemplazados sus 3 `fetch` directos) y comparte la instancia `browserSource` con el engine; `BrowserAdapter` cachea por key (la promesa) → los 4 archivos comunes (weapons/warframes/archon-shards/incarnon) se bajan **una sola vez** (mata el doble-fetch). Nota: `erasableSyntaxOnly` prohíbe parameter-properties en constructor (campo explícito). tsc + 108 tests verde + **verificación manual browser OK** (usuario 2026-07-02: navegación arsenal↔equipment, equipar, abrir build → los JSON cargan una sola vez y quedan cacheados). **FASE 1 CERRADA en Slice 3.** El objetivo de carga (OQ-DATA-12: import→fetch, bundle, puerto único, sin doble-fetch) está cumplido. **Slice 4 (a) subir merge weapon-stats a `normalize` + (b) sacar `loadEngineData` de `fixtures/` → REASIGNADOS a Fase 2** (corte 2026-07-02): ambos tocan `getDNA`/`ItemRepository`/estructura de `@core` que Fase 2 reestructura (segmentar Weapon/Warframe, sacar responsabilidad C) → hacerlos ahora sería doble-touch; (a) además es la sub-decisión abierta "contrato de entrada del engine" de OQ-DATA-9. Próxima parada de la campaña: **Fase 2** (repos→sintetizadores puros + segmentar + helper `token→Modifier` + absorber Slice 4 a/b) — PERO el usuario prioriza primero una **sesión de cambio de entorno** (proyecto standalone + git; ver [[project-environment]]). **Backlog anotado (usuario, 2026-06-16, a tratar en sus fases):** (a) la estructura de las **intenciones** huele incoherente — `Ensemble` mezcla `slots`+`arcanes`, y la forma de las intent en general pide una estructura más coherente → revisión de la **Capa A** (encaja con Fase 2 / pariente de "A2 nunca construido"); (b) **renombrar `Audit*`** (`AuditStep`/`AuditResponse`/`getAuditResponse`/`audit_session`) — "audit" no comunica nada; el nombre debería referir a su consumidor real, el **oráculo** (Fase 3). Rama `refactor/core-stage0-restructure`.
>
> **[2026-07-02] Gobernanza — sesión "cambio de entorno" (standalone+git) CERRADA.** Priorizada por el usuario antes de Fase 2 (ver entrada anterior). Cierra las 4 decisiones planteadas: qué se versiona, `.dockerignore`, árbol final, orden de migración — resueltas por debate + auditoría real del repo, no a priori. **Topología: mismo `origin` (`github.com/FudouRyouho/OmniFrame`), dos clones independientes** (Linux operativo hoy; Windows pendiente hasta que se reinstale limpio en `/shared/*`) — no hay migración de historia real que hacer. `Project/public/data` (JSON runtime de `BrowserAdapter`) se versiona, confirmado como simplificación deliberada (dirección futura de separarlo a backend+API propia anotada, no desarrollada — ver memoria `project-future-ecosystem-split`). **Fixes aplicados:** (1) `.gitignore` — path drift real (`docs/domains/data/pipeline/audits/` nunca hizo match con el path real `docs/data/pipeline/audits/`) corregido + `git rm --cached` de `source-change-report.json` (commit `2202e2b`). (2) `.dockerignore` — agregado `warframe-items/` (1.8 GB, 14.353 archivos) + higiene (`docs-archive/`, `references/`, `.working/`); archivo local/gitignored, sin commit. (3) `.git` — 430 MB → 37 MB (`git gc`, garbage de `tmp_pack_*` sueltos; `git fsck` confirmó objetos inalcanzables reales protegidos por reflog, sin relación). (4) `docker-compose.yml`/`Dockerfile`/`.dockerignore`/`.editorconfig` confirmados gitignorados a propósito (config local por SO); Windows deberá escribir los suyos al clonar. **Debate Docker vs runtime nativo (motivado por el dolor de rebuilds de "horas"):** medido y resuelto — la causa real era el build-context arrastrando los 14.353 archivos de `warframe-items/` en cada build (ya solucionado por el fix (2) de arriba, anterior a que se planteara el debate); rebuild con cache = 16 s, `--no-cache` completo = 1:40 min. Se mantiene Docker (el motivo que lo justificaba —proteger a Windows del mismo `node_modules`— ya no aplica con repos separados, pero CachyOS es rolling-release y Docker sigue pineando la versión de node del proyecto, cosa que el host no hace). Pendiente barato no ejecutado: declarar `engines` en `package.json` (hoy no existe; refuerzo aunque se corra nativo). **Memorias:** SoT de `memory/` migrado de `/HDD/claude-memories` (btrfs, Linux-only) a `/shared/claude-memories` (NTFS, accesible desde ambos SO) — hecho en Linux, symlink repuntado y verificado; junction de Windows queda pendiente para cuando ese SO exista. Repaso y saneamiento de memorias obsoletas del modelo dual-boot viejo (borrada `project-dualboot-git-permisos`, recortada `project-vitest-en-windows`, reescrita `project-environment` por contradicción interna). **Próxima sesión: Fase 2** de la campaña de saneamiento de `@core` (repos→sintetizadores puros + segmentar Weapon/Warframe + helper `token→Modifier`, absorbe Slice 4 a/b reasignado). Ver memoria `project-environment` para el detalle completo de la auditoría.
>
>
> **[2026-07-02] Engine — Saneamiento de `@core`, Fase 2 en curso (repos→sintetizadores + segmentación + helper compartido).** Retomada tras el cierre de la sesión "cambio de entorno" (ver entrada anterior). Auditoría real del código (no solo memoria) antes de tocar nada: confirmó que `ItemRepository` es static-class con 3 Maps mutables (`items`, `overrides` = bodega ajena de mod-overrides consumida por `ModRepository.getModOverride`, `weaponAttackOverrides` = dato de arma consumido internamente por `resolveMultishot`/`resolvePunchThrough` dentro de `getDNA`); son **6 repos** con el patrón static+Map (no 4 — se suman `DnaRepository` passthrough y `EnemyRepository` de otro dominio), de los cuales **4** (`Mod`/`Arcane`/`Incarnon`/`Shard`) duplican casi literal la lógica token→Modifier; **corrección a la nota de planificación:** `DataLoader.isReady()` está centralizado en el bootstrap, no disperso en cada repo. Plan de 5 slices por dependencia real (no el orden ingenuo original): **E** (independiente) → **A** (mecánico) → **D** (depende de A, ambos tocan `ModRepository.ts`) → **B** (extrae `normalize` de `getDNA`, incluye `weaponAttackOverrides` en su scope — **no** se mueve a `ModRepository`, ese fue un error de scope corregido antes de ejecutar) → **C** (segmentar Weapon/Warframe, depende de B). **Slice E ✅ (2026-07-02):** `loadEngineData` sale de `fixtures/` → `@core/engine/bootstrap/engine-data.ts`; `fixtures/` ya solo aloja `builds.ts` (intenciones). 13 call-sites actualizados (tests, `main.tsx`, `oracle.ts`, comentarios en `DataLoader.ts`/`consume.ts`). Cierra el eje (a) de `OQ-ENGINE-9` y ambos pendientes de `OQ-DATA-12` (ahora CERRADA — el otro pendiente, fetch lazy, ya lo había cerrado Fase 1). tsc + 108 tests verde. Debate previo (mismo día): se planteó si el grafo genérico de `AttributeNode` (composición) debería ser jerarquía de tipos entidad→subtipo — **no se reabre**: los mods son transversales por diseño del juego (mismo token aplica a arma/warframe/companion), el diseño actual ya sigue el patrón ECS por esa razón específica del dominio, y Fase 2 no toca el grafo (solo B, el adaptador raw→DNA). Lo real a vigilar es que 3 de las 6 "decisiones de blindaje" de `arch-decisions.md` §4 siguen sin implementar (Delta Stream, Layered Logic Decorators, Casting Snapshots) — no urgente hoy, anotado para revisar cuando el timeline scrubbing o el layering con orden crítico empiecen a doler. Rama `refactor/core-stage0-restructure`.
>
> **Modelo arquitectónico:** Ver `docs/domains/engine/design/simulation-architecture.md` para el modelo de 5 capas (A / B / C1 / C2 / D) acordado en 2026-05-19. Este documento describe la estructura física; la arquitectura conceptual y los principios de comunicación entre capas están allí.

---

## 1. Estructura física (`Project/src/`)

### Núcleo del motor

| Ruta | Estado | Descripción |
|---|---|---|
| `core/engine/loadout.ts` | **ELIMINADO (2026-05-21)** | `LoadoutState` y `LoadoutIntent` eliminados junto con la vía legacy de `MutatorBridge`. |
| `core/engine/formulas/` | **Casi vacío (reservado)** | Subdirs por categoría (ability, arcane, weapon, warframe, common) en su mayoría vacíos a propósito — reservados para fórmulas dedicadas futuras (ability-like / composición no derivable del snapshot). `arcane/arcane-core.ts` purgado (2026-06-11, shape stale). `crit-base.ts` / `scaling-base.ts` siguen activos. |
| `core/engine/contracts/` | **Activo** | Contratos del motor. Split 2026-06-12: `contracts.ts` (cortes/DTOs) + `primitives.ts` (`AttributeNode`, `Modifier`, `GameLaws`, ids) + barrel `index.ts`; más `damage-logic.ts`, `damage-multipliers.ts`, `mod-overrides.ts`. (`attributes.ts` eliminado en refactor 2026-05-21.) |
| `core/bridge/` (B) + `core/engine/resolve/` (C1) + `core/engine/simulate/` (C2) | **Activo** | Implementación del motor, reorganizada 2026-06-12 (DC-OQ-ENGINE-9): `bridge/MutatorBridge` (B, fuera de engine); `resolve/SimulationEngine` + `resolve/hydration/{StaticHydrator,ModRepository,IncarnonRepository,ArcaneRepository,DnaRepository,DataLoader,DamageCombiner,…}` (C1); `simulate/combat/{CombatCalculator,StatusEngine,TimelineSimulator,AtomicSimulator,…}` + `simulate/enemies/` (C2). `EnsembleAdapter` eliminado (2026-05-19). Contrato de intención: `EnsembleIntention.arcanes` + `Ensemble.{warframe,WeaponIntent}.arcanes` (slot dedicado, hermano de `mods`). |
| `core/intention/ensemble-store.ts` (A1) | **Activo** | `ensembleStore` movido aquí desde `providers/Ensemble/` (2026-06-12). SSoT de intención del usuario; observable agnóstico a React. |
| `core/engine/hooks/useSimulation.ts` | **Activo** | Hook React (D-parcial) que conecta `ensembleStore` al motor vía `MutatorBridge`. Drift a reubicar fuera de `@core` cuando D se materialice (Stage 2 gated). |
| `core/engine/__tests__-legacy/` | **ELIMINADO** | 12 suites de test purgadas en sesión anterior. |
| `core/engine/__tests__/` | **Activo** | Suites gold standard (Vitest), consumidores derivados del clic. Cobertura: 6 archivos (Boltor/Cedo/Felarx/Laetum/Lanka + arcano v0), ~63 tests. La deuda `IncarnonRepository` (lectura `upgrades[]` vs `stats[]` D-18) **resuelta** (Capa 3 cerrada 2026-06-06; el repo lee `stats[]`). Índice vivo: [`engine/test/catalog-current.md`](../domains/engine/test/catalog-current.md). |

### Providers — Estado de transición

> ⚠️ Esta capa está en transición. Existen DOS sistemas de estado paralelos sin sincronizar.

| Provider | Estado | Descripción |
|---|---|---|
| `providers/Ensemble/` | **Activo — sistema nuevo** | `EnsembleProvider.tsx`: binding React (capa de composición). Importa `@core/intention/ensemble-store` (ruling `@providers→@core` permitido, 2026-06-12). El store en sí (`ensembleStore`, A1) vive ahora en `@core/intention/`; el contrato de intención (`ensemble.types`) en `@shared/types/ensemble.ts`. |
| `providers/Loadout/` | **ELIMINADO (2026-05-19)** | `LoadoutContext` y `LoadoutProvider` purgados. `LoadoutState` y `loadout.ts` también eliminados (2026-05-21). Sin remanentes del sistema legacy. Decisión: OQ-STATE-1/3/4. |
| `providers/DataState/` | **Activo** | Context headless de estado de UI (concepto `data-*` HTML). No relacionado con el engine. |
| `providers/Shell/` | **Activo** | Navegación, zona, título y footer del shell. |
| `providers/Menu/` | **Activo** | Estado del menú de navegación. |
| `providers/Theme/` | **Activo** | Paleta, colores y selector de tema. |

### La capa de integración — `MutatorBridge` (Capa B)

> ⚠️ **Histórico corregido:** `EnsembleAdapter` fue eliminado en 2026-05-19 (OQ-STATE-4). La descripción anterior era drift.

`MutatorBridge` (`engine/bridge/`) es la capa B del modelo de 5 capas — orquesta la simulación completa desde `EnsembleIntention`:

- Absorbe la lógica que `EnsembleAdapter` tenía como stub (`fromIntention`)
- Traduce intención → contratos del engine (C1) sin conocer la UI
- Comunicación unidireccional: A → B → C1/C2 → D

`useSimulation` es la implementación parcial de la Capa D (Proyección) — conecta `EnsembleStore` → `MutatorBridge` → UI. El contrato formal `ViewModelContract` está pendiente.

### Dominios de UI

| Ruta | Estado | Descripción |
|---|---|---|
| `domains/arsenal/` | **Stub** | Estado de sesión UI-local en `arsenal-ui-session.ts` + `use-arsenal-ui-session.ts` (`useArsenalUiSession`), tras purga de la mitad `arsenalMetadata` mock (Stage 1) + rename honesto del store (Stage 2) — `DC-OQ-STUB-1`/OQ-UI-2. Intención vía `useEnsemble`. `UpgradeView` sin diseño definido. |
| `domains/equipment/` | **Activo** | Controlador de composición delegado a `@shared`. |
| `domains/hud/` | **Activo** | `Hud`, `HudHeader`, footer por zona (Arsenal, Equipment, Items). |
| `domains/options/` | **Activo** | Vista de opciones. |
| `domains/profile/` | **Activo** | Vista de perfil. |

### Shared

| Ruta | Estado | Descripción |
|---|---|---|
| `shared/data/DataRegistry.ts` | **Activo (UI)** | SSoT de acceso a datos en runtime para la UI. Carga y cachea datasets por tipo con hidratación de imágenes. Candidato a evolucionar hacia el DataLoader singleton (ver `OQ-DATA-3`). |
| `shared/types/` | **Activo** | 13 módulos de contrato TypeScript: `ability`, `arcane`, `archwing-weapon`, `base`, `companion`, `damage`, `ensemble` (gemelo-de-entrada / cut A, movido desde `providers/Ensemble` 2026-06-12), `mod`, `polarity`, `stats`, `vehicle`, `warframe`, `weapon`. |
| `shared/components/` | **Activo** | Sistema de vistas unificado: cards, specs/detail views, views por entidad, filters/toolbars, navigation, popovers, slots. |
| `shared/hooks/` | **Activo** | `use-items.ts` (data), `use-performance-debug.ts` (debug). |

---

## 2. Gaps pendientes de resolución

| Gap | Estado |
|---|---|
| Arsenal es cliente real del engine | ⚠️ Pendiente — store de arsenal purgado a estado de sesión UI-local honesto (`use-arsenal-ui-session.ts`, Stage 1+2); intención vía `useEnsemble`, pero UpgradeView sin diseño definido |
| Capa D materializada (`C→D→UI`) | 🟡 v0 display-only (C1): `ViewModelContract` + `project()` en `@shared/view-model`, consumido por D2 (oráculo `view`) y D1 (`UpgradeView` vía `useViewModel`). Falta i18n/formatter, `metrics`/A2 (C2) |
| `UpgradeView` importa `@core` directo | ✅ **RESUELTO (2026-06-12)** — consume `ViewModelContract` vía `useViewModel` (`@providers`); ningún dominio importa `@core` |
| Multi-config builds (A/B/C por entidad) | ⚪ Diferido por prioridad — **NO hay cimiento ausente**: el flujo A→B→C lo proyecta-permitir; se modeló **1-config por simplicidad**. Target *warframe-like* (extensión de A: anidar N config slots compartiendo polaridades + A maneja "cuál activa ahora"; badge A/B/C en `/equipment/*`; **≠** builds-separadas overframe.gg; Profile ≠ builds). Costoso/invasivo en B→C. Ver entrada [2026-06-16] + `OQ-UI-2` |

---

## 3. Preguntas abiertas críticas

**OQ-ENGINE-2: Profile switching (Incarnon/Alt-fire) — ABIERTO**
Re-hidratar todo el ensemble vs. conmutar `active_profile_id` en `resolve()` en runtime. Ver `docs/governance/open-questions.md`.

OQs cerradas: ver `docs/governance/closed-decisions.md`.

---

## 4. Lo que está estable y no debe tocarse

- `core/engine/formulas/` — fórmulas matemáticas probadas
- `core/engine/contracts/` — contratos del motor ratificados
- `shared/types/` — contratos TypeScript del dominio
- `shared/components/` — sistema de vistas unificado
- `shared/data/DataRegistry.ts` — carga de datos en runtime
- `providers/Shell/`, `Menu/`, `Theme/`, `DataState/` — providers de UI estables
