/**
 * @domain Engine / Contracts
 *
 * Barrel: re-exporta primitivos (vocabulario no-frontera) y contratos
 * (cortes de frontera entre capas). La superficie pública es idéntica a
 * la del monolito previo — los call-sites no cambian.
 */

export * from './primitives';
export * from './contracts';
